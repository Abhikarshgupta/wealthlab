# Adversarial fixtures (DS-6)

Synthetic bad payloads for **unit tests** and mutation testing. Not duplicated exhaustively in Gherkin — pick representative `@edge` scenarios.

| File | Use |
|------|-----|
| `null-undefined.json` | Null/undefined/type confusion |

Add `overflow.json`, `corrupt-localStorage.json` in later waves.
